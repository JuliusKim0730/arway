---
title: "5.00 목적지 선택 화면 구현"
type: "implementation"
tags: ["frontend", "nextjs", "react", "ui", "destination"]
dependencies: ["nextjs", "react", "typescript", "axios"]
---

# 5.00 목적지 선택 화면 구현

## 워크플로우 개요

사용자가 목적지를 선택할 수 있는 화면을 구현하고, 선택한 목적지 정보를 저장합니다.

## 목표

- 목적지 목록 표시
- 목적지 카드 UI 구현
- 목적지 선택 및 상태 관리
- 선택한 목적지 정보 저장
- 네비게이션 세션 생성

## 단계별 작업

### 5.1 목적지 목록 페이지

#### 5.1.1 페이지 컴포넌트
`frontend/app/ar-nav/select/page.tsx`:
```typescript
'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';
import { fetchDestinations, createSession } from '@/lib/api';

interface Destination {
  id: string;
  name: string;
  description: string;
  latitude: number;
  longitude: number;
  address?: string;
}

export default function ArNavSelectPage() {
  const router = useRouter();
  const [destinations, setDestinations] = useState<Destination[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    loadDestinations();
  }, []);

  const loadDestinations = async () => {
    try {
      const data = await fetchDestinations();
      setDestinations(data);
      setLoading(false);
    } catch (err) {
      setError('목적지를 불러오는데 실패했습니다.');
      setLoading(false);
    }
  };

  const handleSelectDestination = async (destination: Destination) => {
    try {
      // 현재 위치 가져오기
      const position = await getCurrentPosition();
      
      // 테스트용 사용자 ID (실제로는 인증된 사용자 ID 사용)
      const userId = 'test-user-id';
      
      // 네비게이션 세션 생성
      const session = await createSession({
        user_id: userId,
        destination_id: destination.id,
        start_latitude: position.coords.latitude,
        start_longitude: position.coords.longitude,
      });

      // 세션 ID를 로컬 스토리지에 저장
      localStorage.setItem('currentSessionId', session.id);
      localStorage.setItem('targetLatLng', JSON.stringify({
        lat: destination.latitude,
        lng: destination.longitude,
      }));

      // AR 네비 실행 화면으로 이동
      router.push('/ar-nav/run');
    } catch (err) {
      alert('위치 정보를 가져올 수 없습니다. 위치 권한을 확인해주세요.');
    }
  };

  const getCurrentPosition = (): Promise<GeolocationPosition> => {
    return new Promise((resolve, reject) => {
      if (!navigator.geolocation) {
        reject(new Error('Geolocation is not supported'));
        return;
      }

      navigator.geolocation.getCurrentPosition(
        resolve,
        reject,
        { enableHighAccuracy: true, timeout: 5000 }
      );
    });
  };

  if (loading) {
    return (
      <div className="min-h-screen bg-gray-900 text-white flex items-center justify-center">
        <p>목적지를 불러오는 중...</p>
      </div>
    );
  }

  if (error) {
    return (
      <div className="min-h-screen bg-gray-900 text-white flex items-center justify-center">
        <p className="text-red-400">{error}</p>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gray-900 text-white">
      {/* 헤더 */}
      <header className="p-6 flex items-center">
        <Link href="/ar-nav" className="mr-4">
          <span className="text-2xl">←</span>
        </Link>
        <h1 className="text-xl font-semibold">목적지 선택</h1>
      </header>

      {/* 목적지 카드 리스트 */}
      <main className="px-6 pb-6">
        <div className="space-y-4">
          {destinations.map((destination) => (
            <DestinationCard
              key={destination.id}
              destination={destination}
              onSelect={() => handleSelectDestination(destination)}
            />
          ))}
        </div>

        {/* 하단 안내문구 */}
        <div className="mt-8 p-4 bg-gray-800 rounded-lg text-sm text-gray-400 text-center">
          MVP 버전에서는 1~2개 고정된 목적지만 지원합니다.
          <br />
          실제 버전에서는 지도 검색, 즐겨찾기 등으로 확장됩니다.
        </div>
      </main>
    </div>
  );
}
```

### 5.2 목적지 카드 컴포넌트

#### 5.2.1 카드 컴포넌트
`frontend/components/DestinationCard.tsx`:
```typescript
interface Destination {
  id: string;
  name: string;
  description: string;
  latitude: number;
  longitude: number;
  address?: string;
}

interface DestinationCardProps {
  destination: Destination;
  onSelect: () => void;
}

export default function DestinationCard({ destination, onSelect }: DestinationCardProps) {
  return (
    <div className="bg-gray-800 rounded-lg p-6 border border-gray-700">
      <h2 className="text-xl font-semibold mb-2">{destination.name}</h2>
      <p className="text-gray-400 mb-4">{destination.description}</p>
      {destination.address && (
        <p className="text-sm text-gray-500 mb-4">{destination.address}</p>
      )}
      <button
        onClick={onSelect}
        className="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-4 rounded-lg transition-colors"
      >
        이 위치로 안내
      </button>
    </div>
  );
}
```

### 5.3 상태 관리 개선

#### 5.3.1 Zustand 스토어 설정
`frontend/store/navigationStore.ts`:
```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface TargetLocation {
  lat: number;
  lng: number;
}

interface NavigationState {
  currentSessionId: string | null;
  targetLocation: TargetLocation | null;
  setSessionId: (id: string) => void;
  setTargetLocation: (location: TargetLocation) => void;
  clearNavigation: () => void;
}

export const useNavigationStore = create<NavigationState>()(
  persist(
    (set) => ({
      currentSessionId: null,
      targetLocation: null,
      setSessionId: (id) => set({ currentSessionId: id }),
      setTargetLocation: (location) => set({ targetLocation: location }),
      clearNavigation: () => set({ currentSessionId: null, targetLocation: null }),
    }),
    {
      name: 'navigation-storage',
    }
  )
);
```

### 5.4 에러 핸들링 개선

#### 5.4.1 에러 바운더리
`frontend/components/ErrorBoundary.tsx`:
```typescript
'use client';

import { Component, ReactNode } from 'react';

interface Props {
  children: ReactNode;
}

interface State {
  hasError: boolean;
}

export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError() {
    return { hasError: true };
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="min-h-screen bg-gray-900 text-white flex items-center justify-center">
          <div className="text-center">
            <h2 className="text-xl font-semibold mb-4">오류가 발생했습니다</h2>
            <button
              onClick={() => window.location.reload()}
              className="bg-blue-600 text-white px-4 py-2 rounded"
            >
              새로고침
            </button>
          </div>
        </div>
      );
    }

    return this.props.children;
  }
}
```

## 검증 체크리스트

- [ ] 목적지 목록 API 호출 구현 완료
- [ ] 목적지 카드 UI 구현 완료
- [ ] 목적지 선택 기능 구현 완료
- [ ] 현재 위치 가져오기 구현 완료
- [ ] 네비게이션 세션 생성 완료
- [ ] 선택한 목적지 정보 저장 완료
- [ ] 에러 핸들링 구현 완료
- [ ] 로딩 상태 표시 완료

## 다음 단계

다음 워크플로우: **6.00 AR 네비 실행 화면 구현 (핵심)**

## 참고 자료

- Next.js 라우팅: https://nextjs.org/docs/app/building-your-application/routing
- Geolocation API: https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API
- Zustand 문서: https://zustand-demo.pmnd.rs/

