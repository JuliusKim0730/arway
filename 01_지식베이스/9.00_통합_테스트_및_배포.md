---
title: "9.00 통합 테스트 및 배포"
type: "implementation"
tags: ["testing", "deployment", "integration", "poc"]
dependencies: ["jest", "pytest", "docker"]
---

# 9.00 통합 테스트 및 배포

## 워크플로우 개요

전체 시스템의 통합 테스트를 수행하고, 로컬 PoC 환경에 배포합니다.

## 목표

- 단위 테스트 작성
- 통합 테스트 수행
- E2E 테스트 시나리오 검증
- 로컬 배포 스크립트 작성
- 문서화 완료

## 단계별 작업

### 9.1 백엔드 테스트

#### 9.1.1 단위 테스트
`backend/tests/test_api.py`:
```python
import pytest
from fastapi.testclient import TestClient
from app.main import app
from app.database import SessionLocal, Base, engine

client = TestClient(app)

@pytest.fixture(scope="function")
def db_session():
    Base.metadata.create_all(bind=engine)
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
    Base.metadata.drop_all(bind=engine)

def test_create_destination(db_session):
    response = client.post(
        "/api/v1/destinations/",
        json={
            "name": "Test Destination",
            "latitude": 37.511,
            "longitude": 127.029,
            "created_by": "test-user-id"
        }
    )
    assert response.status_code == 200
    data = response.json()
    assert data["name"] == "Test Destination"

def test_get_destinations(db_session):
    response = client.get("/api/v1/destinations/")
    assert response.status_code == 200
    assert isinstance(response.json(), list)
```

#### 9.1.2 테스트 실행
```bash
cd backend
pytest tests/ -v
```

### 9.2 프론트엔드 테스트

#### 9.2.1 컴포넌트 테스트
`frontend/__tests__/ArNavStartPage.test.tsx`:
```typescript
import { render, screen } from '@testing-library/react';
import { useRouter } from 'next/navigation';
import ArNavStartPage from '@/app/ar-nav/page';

jest.mock('next/navigation');

describe('ArNavStartPage', () => {
  it('renders start button', () => {
    (useRouter as jest.Mock).mockReturnValue({
      push: jest.fn(),
    });
    
    render(<ArNavStartPage />);
    expect(screen.getByText('도보 AR 네비 시작')).toBeInTheDocument();
  });
});
```

#### 9.2.2 테스트 실행
```bash
cd frontend
npm test
```

### 9.3 통합 테스트 시나리오

#### 9.3.1 E2E 테스트 시나리오
`tests/e2e/navigation-flow.test.ts`:
```typescript
describe('Navigation Flow E2E', () => {
  it('should complete full navigation flow', async () => {
    // 1. 시작 화면 접근
    await page.goto('http://localhost:3000/ar-nav');
    
    // 2. 시작 버튼 클릭
    await page.click('text=도보 AR 네비 시작');
    
    // 3. 목적지 선택
    await page.waitForSelector('text=테스트 목적지 1');
    await page.click('text=이 위치로 안내');
    
    // 4. AR 네비 화면 확인
    await page.waitForSelector('text=m');
    
    // 5. 도착 테스트 버튼 클릭
    await page.click('text=도착 테스트');
    
    // 6. 도착 화면 확인
    await page.waitForSelector('text=도착했습니다');
  });
});
```

### 9.4 배포 스크립트

#### 9.4.1 Docker Compose 설정
`docker-compose.yml`:
```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: arway_lite
      POSTGRES_USER: arway_user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://arway_user:password@postgres:5432/arway_lite
    depends_on:
      - postgres
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - backend
    volumes:
      - ./frontend:/app

volumes:
  postgres_data:
```

#### 9.4.2 배포 스크립트
`scripts/deploy.sh`:
```bash
#!/bin/bash

echo "🚀 ARWay Lite 배포 시작..."

# 1. 데이터베이스 마이그레이션
echo "📦 데이터베이스 마이그레이션 실행..."
cd backend
alembic upgrade head

# 2. 시드 데이터 생성
echo "🌱 시드 데이터 생성..."
python app/database/seeds.py

# 3. Docker Compose 시작
echo "🐳 Docker Compose 시작..."
cd ..
docker-compose up -d

echo "✅ 배포 완료!"
echo "Frontend: http://localhost:3000"
echo "Backend API: http://localhost:8000"
echo "Admin: http://localhost:3000/admin"
```

### 9.5 문서화

#### 9.5.1 README 작성
`README.md`:
```markdown
# ARWay Lite - SCQ 기반 AR 도보 네비게이션 MVP

## 빠른 시작

### 사전 요구사항
- Node.js 18+
- Python 3.11+
- PostgreSQL 15+
- Docker (선택사항)

### 설치 및 실행

1. 저장소 클론
2. 환경 변수 설정
3. 의존성 설치
4. 데이터베이스 마이그레이션
5. 서버 시작

자세한 내용은 각 워크플로우 문서를 참조하세요.
```

## 검증 체크리스트

### 기능 검증
- [ ] 시작 화면 정상 동작
- [ ] 목적지 선택 정상 동작
- [ ] AR 네비게이션 정상 동작
- [ ] GPS 추적 정상 동작
- [ ] 도착 화면 정상 동작
- [ ] 피드백 제출 정상 동작
- [ ] Admin 대시보드 정상 동작

### 성능 검증
- [ ] 화면 렌더링 30fps 이상
- [ ] API 응답 시간 <200ms
- [ ] GPS 업데이트 주기 1초

### 데이터 검증
- [ ] 데이터베이스 연결 정상
- [ ] 세션 데이터 저장 정상
- [ ] GPS 포인트 저장 정상
- [ ] 피드백 저장 정상

### 보안 검증
- [ ] CORS 설정 확인
- [ ] SQL Injection 방지 확인
- [ ] 입력 검증 확인

## 배포 체크리스트

- [ ] 모든 테스트 통과
- [ ] 환경 변수 설정 완료
- [ ] 데이터베이스 마이그레이션 완료
- [ ] 시드 데이터 생성 완료
- [ ] Docker Compose 설정 완료
- [ ] 문서화 완료

## 다음 단계

PoC 완료 후:
1. 사용자 피드백 수집
2. 성능 최적화
3. SCQ 모델 통합 준비
4. 프로덕션 배포 계획 수립

## 참고 자료

- Jest 문서: https://jestjs.io/
- Pytest 문서: https://docs.pytest.org/
- Docker Compose 문서: https://docs.docker.com/compose/

