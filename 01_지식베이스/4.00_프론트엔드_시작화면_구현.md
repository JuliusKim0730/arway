---
title: "4.00 프론트엔드 시작 화면 구현"
type: "implementation"
tags: ["frontend", "nextjs", "react", "ui", "landing"]
dependencies: ["nextjs", "react", "typescript", "tailwindcss"]
---

# 4.00 프론트엔드 시작 화면 구현

## 워크플로우 개요

Next.js를 사용하여 AR 네비게이션 시작 화면을 구현합니다.

## 목표

- 시작 화면 레이아웃 구현
- 서비스 소개 UI 구성
- 네비게이션 라우팅 설정
- 반응형 디자인 적용

## 단계별 작업

### 4.1 페이지 구조 생성

#### 4.1.1 라우트 생성
`frontend/app/ar-nav/page.tsx`:
```typescript
'use client';

import { useRouter } from 'next/navigation';
import Link from 'next/link';

export default function ArNavStartPage() {
  const router = useRouter();

  const handleStart = () => {
    router.push('/ar-nav/select');
  };

  return (
    <div className="min-h-screen bg-gray-900 text-white flex flex-col">
      {/* 헤더 */}
      <header className="p-6 text-center">
        <h1 className="text-3xl font-bold mb-2">ARWay Lite</h1>
        <p className="text-gray-400 text-sm">SCQ 기반 AR 도보 네비게이션 실험</p>
      </header>

      {/* 설명 섹션 */}
      <main className="flex-1 flex flex-col items-center justify-center px-6">
        <div className="max-w-md text-center space-y-6">
          <p className="text-lg leading-relaxed">
            지도를 보지 않고도, 카메라 화면 위에서 방향을 안내받는 실험용 서비스입니다.
          </p>
          <p className="text-sm text-gray-400">
            현재는 도보 테스트용으로만 사용해주세요.
          </p>
        </div>
      </main>

      {/* 메인 CTA 버튼 */}
      <div className="p-6">
        <button
          onClick={handleStart}
          className="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-4 px-6 rounded-lg transition-colors"
        >
          도보 AR 네비 시작
        </button>
      </div>

      {/* 푸터 */}
      <footer className="p-6 text-center text-sm text-gray-500">
        <p>카메라/위치 권한을 허용해야 사용할 수 있습니다.</p>
        <p className="mt-2">베타 버전 - 실험용 서비스</p>
      </footer>
    </div>
  );
}
```

### 4.2 레이아웃 컴포넌트

#### 4.2.1 공통 레이아웃
`frontend/app/layout.tsx`:
```typescript
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'ARWay Lite - AR 도보 네비게이션',
  description: 'SCQ 기반 AR 도보 네비게이션 MVP',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ko">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

### 4.3 스타일 설정

#### 4.3.1 TailwindCSS 설정
`frontend/tailwind.config.ts`:
```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#2563eb',
          dark: '#1e40af',
        },
      },
    },
  },
  plugins: [],
};
export default config;
```

#### 4.3.2 글로벌 스타일
`frontend/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --foreground-rgb: 255, 255, 255;
  --background-start-rgb: 17, 24, 39;
  --background-end-rgb: 17, 24, 39;
}

body {
  color: rgb(var(--foreground-rgb));
  background: linear-gradient(
      to bottom,
      transparent,
      rgb(var(--background-end-rgb))
    )
    rgb(var(--background-start-rgb));
}
```

### 4.4 API 클라이언트 설정

#### 4.4.1 API 유틸리티
`frontend/lib/api.ts`:
```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000';

export async function fetchDestinations() {
  const response = await fetch(`${API_URL}/api/v1/destinations/`);
  if (!response.ok) {
    throw new Error('Failed to fetch destinations');
  }
  return response.json();
}

export async function createSession(data: {
  user_id: string;
  destination_id: string;
  start_latitude: number;
  start_longitude: number;
}) {
  const response = await fetch(`${API_URL}/api/v1/sessions/`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) {
    throw new Error('Failed to create session');
  }
  return response.json();
}
```

### 4.5 권한 요청 훅

#### 4.5.1 권한 관리 훅
`frontend/hooks/usePermissions.ts`:
```typescript
import { useState, useEffect } from 'react';

interface Permissions {
  geolocation: boolean;
  camera: boolean;
}

export function usePermissions() {
  const [permissions, setPermissions] = useState<Permissions>({
    geolocation: false,
    camera: false,
  });

  useEffect(() => {
    // Geolocation 권한 확인
    if ('geolocation' in navigator) {
      navigator.permissions?.query({ name: 'geolocation' }).then((result) => {
        setPermissions((prev) => ({
          ...prev,
          geolocation: result.state === 'granted',
        }));
      });
    }

    // Camera 권한 확인
    navigator.mediaDevices
      .getUserMedia({ video: true })
      .then(() => {
        setPermissions((prev) => ({ ...prev, camera: true }));
      })
      .catch(() => {
        setPermissions((prev) => ({ ...prev, camera: false }));
      });
  }, []);

  return permissions;
}
```

## 검증 체크리스트

- [ ] 시작 화면 레이아웃 구현 완료
- [ ] 서비스 소개 텍스트 표시 완료
- [ ] 시작 버튼 클릭 시 라우팅 동작 확인
- [ ] 반응형 디자인 적용 완료
- [ ] 다크 테마 스타일 적용 완료
- [ ] 권한 안내 문구 표시 완료
- [ ] API 클라이언트 설정 완료

## 다음 단계

다음 워크플로우: **5.00 목적지 선택 화면 구현**

## 참고 자료

- Next.js 공식 문서: https://nextjs.org/docs
- TailwindCSS 문서: https://tailwindcss.com/docs
- React Hooks 문서: https://react.dev/reference/react

